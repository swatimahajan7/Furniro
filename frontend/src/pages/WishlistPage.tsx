import { ComingSoon, PageShell } from '@/components/layout';

export default function WishlistPage() {
  return (
    <PageShell title="Wishlist">
      <ComingSoon phase={5} what="Your wishlist" />
    </PageShell>
  );
}
