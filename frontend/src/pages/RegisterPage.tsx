import { ComingSoon, PageShell } from '@/components/layout';

export default function RegisterPage() {
  return (
    <PageShell title="Create Account" name="register">
      <ComingSoon phase={5} what="Account registration" />
    </PageShell>
  );
}
