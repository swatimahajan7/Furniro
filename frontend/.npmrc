# registry.npmjs.org is unreachable from the primary dev network (TLS reset), so we use Yarn's
# public mirror of the same registry. It serves identical packages and tarball checksums.
registry=https://registry.yarnpkg.com/
engine-strict=true
